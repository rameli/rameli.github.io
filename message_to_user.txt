I've further reduced the width of the left navigation column to `col-lg-1` (approximately 8.33% of the screen width) and adjusted the main content to `col-lg-11`. Please perform a hard refresh in your browser to see these changes. This is the narrowest possible setting using standard Bootstrap grid classes. If this is still not narrow enough, we would need to consider custom CSS for a fixed-pixel width sidebar, which might impact responsiveness.